
export class Drink {
    code: string;
    drinkName: string;
    price: number;
    imageUrl: string;
    added: boolean;

    constructor(code: string,
        drinkName: string,
        price: number,
        imageUrl: string,
        added: boolean) { }
}
